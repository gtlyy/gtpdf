# GtPDF 手动测试清单

## 1. 阅读器基础

- [ ] 打开 PDF：点击「打开」→ 选择文件 → 应正确渲染
- [ ] 最近文件：关闭文件后，点击「打开」→ 弹出最近文件列表 → 显示正确路径
- [ ] 最近文件持久化：关闭程序重开 → 最近文件列表仍在
- [ ] 拖拽打开：拖 PDF 到窗口 → 自动加载
- [ ] 翻页：上一页/下一页按钮 → 页码正确
- [ ] 缩放到 25%/50%/100%/200%/500% → 页面缩放正确
- [ ] 适应宽度 / 适应页面：切换模式 → 自动调整
- [ ] 窗口 resize：拖拽窗口边缘 → 自动适配
- [ ] 全屏：切换全屏 → 正确

## 2. 侧栏（缩略图 + 书签）

- [ ] 缩略图显示：点击「缩略」→ 左侧显示缩略图列表
- [ ] 缩略图滚动：侧栏可独立滚动（不触发页面翻页）
- [ ] 缩略图点击：点击缩略图 → 跳转到对应页
- [ ] 收起侧栏：再次点击「缩略」→ 侧栏收起
- [ ] 书签 tab：打开有书签的 PDF → 点击侧栏「书签」tab → 显示书签树
- [ ] 书签展开/收起：点击有子项的书签 → 展开子树
- [ ] 书签跳转：点击书签 → 跳转到正确页码
- [ ] 无书签 PDF：切换到书签 tab → 显示空/提示

## 3. 搜索

- [ ] 搜索弹窗：点击搜索 → 弹窗正常
- [ ] 搜索高亮：输入关键词 → 匹配项高亮
- [ ] 搜索结果导航：点击上/下结果 → 跳转到对应页
- [ ] 跨页搜索：结果在不同页时 → 翻页后高亮清除（不残留）
- [ ] 关闭搜索：关闭搜索面板 → 高亮清除

## 4. 阅读器交互

- [ ] 平移（中键）：按住鼠标中键拖拽 → 页面移动
- [ ] 平移（工具栏）：点击工具栏「平移」按钮 → 拖拽页面 → 再次点击退出
- [ ] 侧栏自适应缩放：打开/关闭侧栏 → 页面缩放自动调整
- [ ] 侧栏状态持久化：关闭文件重开 → 侧栏状态保持不变

## 5. 标注

- [ ] 高亮：选择高亮工具 → 拖选文字 → 黄色高亮
- [ ] 下划线 / 波浪线 / 删除线：同高亮流程
- [ ] 矩形：选择矩形工具 → 拖拽绘制 → 红色边框
- [ ] 矩形填充：选择填充工具 → 拖拽绘制 → 白色填充矩形
- [ ] 夹批：选择夹批工具 → 点击页面 → 弹出对话框 → 输入文字 → 显示在 PDF 上
- [ ] 夹批移动：拖拽夹批 → 位置更新
- [ ] 夹批颜色/字号：设置不同颜色字号 → 显示正确
- [ ] 9 色调色板：选择标注 → 切换颜色 → 各预设色正常
- [ ] 自由文本：选择自由文本工具 → 点击页面 → 输入文字 → 显示正确
- [ ] 自由文本移动：拖拽自由文本 → 位置更新
- [ ] 自由文本编辑：双击自由文本 → 弹出编辑框 → 修改文字
- [ ] 删除标注：选择橡皮擦 → 点击标注 → 删除
- [ ] 清空标注：点击清空 → 确认 → 全部删除
- [ ] 标注持久化：保存文件 → 重新打开 → 标注仍在

## 6. 笔记（批注）

- [ ] 添加笔记：点击页面 → 弹出笔记输入 → 保存
- [ ] 查看笔记：点击已有笔记标记 → 显示内容
- [ ] 编辑笔记：查看时修改文字 → 保存
- [ ] 删除笔记：查看时删除 → 确认
- [ ] 笔记持久化：关闭文件重开 → 笔记仍在

## 7. 文本选择 + OCR

- [ ] 文字选中：切换划词模式 → 拖选文字 → 蓝色高亮
- [ ] 复制选中：选中后按 Ctrl+C 或右键复制 → 粘贴到记事本验证
- [ ] OCR 选区：扫描 PDF 上开启划词 → 拖选区域 → 识别文字
- [ ] OCR 全页：无文字 PDF 上点击复制 → 自动转 OCR → 显示进度条 → 复制到剪贴板

## 8. 合并

- [ ] 单个文件提取：选择 1 个文件 + 指定页码范围 → 正常提取
- [ ] 多文件合并：选择 2+ 文件 → 生成合并 PDF → 页数正确
- [ ] 页码错误：输入非法页码范围 → 显示友好错误
- [ ] 无文件时点执行 → 提示

## 9. 拆分

- [ ] 按 N 页拆分：拆分 10 页文件为每 3 页一份 → 得到 4 个文件
- [ ] 按指定页拆分：在页 3,7 拆分 → 得到 3 个文件
- [ ] 预览：输入拆分点后 → 预览文本更新

## 10. 重排

- [ ] 选择文件 → 加载页面列表
- [ ] 点击页面 → 选中（高亮）
- [ ] 上移/下移/置顶/置底 → 顺序变化
- [ ] 交换：选中两个页面 → 自动交换
- [ ] 倒序/奇偶分离：点击 → 顺序重排
- [ ] 自定义顺序：输入 `3,1,2` → 页面重排
- [ ] 执行重排 → 输出 PDF 页面顺序正确

## 11. 添加页码

- [ ] 位置：切换 左上/中/右、左下/中/右 → 页码位置正确
- [ ] 字体大小：设置 8/12/24 → 页码大小变化
- [ ] 颜色：选择不同颜色 → 页码颜色正确
- [ ] 起始页、偏移量 → 调整正确
- [ ] 奇偶页不同位置 → 奇数页 bl，偶数页 br → 正确

## 12. 旋转

- [ ] 全部页面旋转 90°/180°/270° → 角度正确
- [ ] 仅奇数/偶数页 → 筛选正确
- [ ] 自定义范围 → 只旋转指定页

## 13. 加密/解密

- [ ] 加密：设密码 → 确认密码一致 → 加密成功
- [ ] 密码不一致：两次密码不同 → 提示「两次密码不一致」
- [ ] 解密加密文件：密码正确 → 解密成功
- [ ] 解密密码错误 → 提示错误
- [ ] 加密已加密文件 → 提示不可二次加密

## 14. 打印

- [ ] 打印对话框 → 调用系统打印
- [ ] 打印页码范围 → 只打印指定页

## 15. Ctrl+J/K 快捷键

- [ ] Ctrl+J → 下一页
- [ ] Ctrl+K → 上一页

## 15. 展开扫描件

- [ ] 有旋转的扫描件：打开 → 提示「检测到旋转页面，是否展开」→ 确认 → 页面展平
- [ ] 自动 DPI 检测：展开后分辨率正确
- [ ] 非扫描件：无旋转 → 不弹出提示

## 16. 右键菜单

- [ ] 复制本页成图：右键 → 复制本页成图 → 粘贴验证
- [ ] 复制全部成图：右键 → 复制全部成图 → 粘贴验证
- [ ] 本页成图（导出）：右键 → 本页成图 → 文件保存正确
- [ ] 本页扣图：右键 → 本页扣图 → 提取图片正确
- [ ] 笔记列表：右键 → 笔记列表 → 显示当前页笔记
- [ ] 夜间模式：右键 → 夜间模式 → 切换
- [ ] 删除本页：右键 → 删除本页 → 页面被删除

## 17. 其他

- [ ] 另存为 → 保存副本
- [ ] 另存后保持当前页：另存 → 不跳回首页
- [ ] 删除当前页 → 页数减少
- [ ] 删除页面后缩略图正常：删除后缩略图列表不报错
- [ ] 导出 PNG（单页/多页） → 导出进度条 → 图片正确
- [ ] 导出合并 → 竖向/横向拼接正确
- [ ] 扣图 → 提取 PDF 内嵌图片
- [ ] 夜间模式 → 切换后黑白反转
- [ ] 最近文件空列表：清空最近文件 → 点打开直接弹出对话框
- [ ] 命令行打开：`./gtpdf file.pdf` → 直接加载
